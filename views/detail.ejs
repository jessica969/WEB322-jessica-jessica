<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Enthusiasts Society</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">About us, <%= user.firstName %>...</h1>
        <div class="mt-4">
            <h5>Member ID: <%= user.id %></h5>
            <h5>Known Alias: <%= user.lastName %></h5>
            <h5>Contact: <%= user.email %></h5>
            <h5>Date of Birth: <%= user.dob.split('T')[0] %></h5>
            <h5>Affiliation: <%= user.company %></h5>
            <h5>Emergency Contact: <%= user.phone %></h5>
        </div>
        <div class="text-center mt-5">
            <button class="btn btn-primary" onclick="history.back()">Return</button>
        </div>
    </div>
</body>
</html>
